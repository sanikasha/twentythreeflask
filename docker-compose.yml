version: '3'
services:
        web:
                image: osflask_port_v1
                build: .
                ports:
                        - "8723:8080"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
